# CLAUDE.md — {{PROJECT_NAME}}

**Purpose**: Main Claude Code configuration for this Nuxt 3 / Vue 3 / TypeScript production application.

This file was generated by [`claude-frontend-cli`](https://github.com/{{KIT_REPO}}) (`cfc init`).
The section between the `claude-frontend-cli` markers is managed by the CLI and refreshed by `cfc sync`.
Add your own project notes **outside** those markers — they will be preserved.

---

## 🚀 Quick Start

```bash
npm install && npm run dev   # Start development
npm run lint && npm run type-check && npm run test:unit   # Quality gates (or /check)
```

| Command | Purpose |
|---------|---------|
| `npm run lint` | ESLint — code quality & style |
| `npm run type-check` | TypeScript verification (vue-tsc) |
| `npm run test:unit` | Vitest unit tests |

---

## 📦 Technology Stack

| Technology | Purpose |
|------------|---------|
| Nuxt 3 | Framework |
| Vue 3 | UI Library |
| TypeScript | Type Safety |
| Pinia | State Management |
| PrimeVue | UI Components |
| Tailwind CSS | Styling |
| Zod | Validation |
| Vitest | Testing |
| i18n | Internationalization |

<!-- Adjust versions and add project-specific packages in .instructions/PROJECT-SETUP.md -->

{{MANAGED_BLOCK}}

---

<!-- Add your own project-specific guidance below — this area is not touched by `cfc sync`. -->
